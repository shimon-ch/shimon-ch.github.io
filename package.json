{
  "name": "shimon-ch.github.io",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "rimraf dist/ && concurrently --raw \"vite\" \"eleventy --serve\"",
    "build": "vite build && cross-env ELEVENTY_ENV=production eleventy && rimraf dist/manifest.json"
  },
  "engines": "16",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "concurrently": "^7.1.0",
    "cross-env": "^7.0.3",
    "prettier": "^2.6.2",
    "prettier-plugin-organize-imports": "^2.3.4",
    "rimraf": "^3.0.2",
    "sass": "^1.51.0",
    "sass-mq": "^6.0.0",
    "vite": "^2.9.8"
  }
}
